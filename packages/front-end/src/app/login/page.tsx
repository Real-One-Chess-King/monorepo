"use client";

import Login from "@/components/platform/login/login.component";
import ApolloLayout from "@/components/apollo.layout";

export default function LoginPage() {
  return (
    <ApolloLayout>
      <Login />
    </ApolloLayout>
  );
}
